/**
 * dashboard 内部颜色工具
 *
 * 说明：
 * - 实现收敛到 @grafana-fast/utils
 * - 该文件仅作为 dashboard 包内部的导出路径（/#+/utils/color）
 */

export { COLOR_SCHEMES, generateChartColors, rgbToHex, hexToRgb, adjustAlpha, getColorByThreshold } from '@grafana-fast/utils';
