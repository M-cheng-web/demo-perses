# 高级图表功能实现计划

## 📋 功能清单（参考 Perses）

### 1. 自定义 Legend 组件
- [ ] 创建 `Legend.vue` 基础组件
- [ ] 实现 `CompactLegend.vue`（tag 标签模式）
- [ ] 实现 `ListLegend.vue`（列表模式）
- [ ] 实现 `TableLegend.vue`（表格模式）
- [ ] 添加虚拟化支持（大量系列时）
- [ ] 实现选中/取消选中交互
- [ ] 实现 hover 高亮效果
- [ ] 集成到图表组件

### 2. 自定义 Tooltip 组件
- [ ] 创建 `ChartTooltip.vue` 基础组件
- [ ] 实现鼠标位置追踪
- [ ] 实现附近系列数据查找
- [ ] 实现智能定位算法
- [ ] 实现深色主题样式
- [ ] 使用 Teleport 渲染到 body

### 3. 固定 Tooltip 功能
- [ ] 添加点击事件监听
- [ ] 实现 pinned state 管理
- [ ] 添加固定/取消固定按钮
- [ ] 实现固定位置保持

### 4. Composables
- [ ] `useMousePosition.ts` - 鼠标位置追踪
- [ ] `useTooltipPosition.ts` - Tooltip 定位计算
- [ ] `useSeriesSelection.ts` - 系列选中管理

## 🎯 实现策略

### 阶段 1：Tooltip（最重要）
从 Tooltip 开始，因为它对用户体验影响最大。

### 阶段 2：Legend
Legend 可以显著改善多系列图表的可读性。

### 阶段 3：优化和完善
添加虚拟化、性能优化等高级功能。

## 📁 文件结构

```
src/
├── components/
│   ├── Charts/
│   │   └── TimeSeriesChart.vue  # 修改：集成自定义组件
│   ├── ChartTooltip/
│   │   ├── ChartTooltip.vue      # 主 Tooltip 组件
│   │   ├── TooltipContent.vue    # Tooltip 内容
│   │   └── TooltipHeader.vue     # Tooltip 头部
│   └── ChartLegend/
│       ├── Legend.vue            # Legend 容器
│       ├── CompactLegend.vue     # Tag 模式
│       ├── ListLegend.vue        # 列表模式
│       └── TableLegend.vue       # 表格模式
├── composables/
│   ├── useMousePosition.ts
│   ├── useTooltipPosition.ts
│   └── useSeriesSelection.ts
└── types/
    ├── chart.ts                  # 添加新类型
    └── legend.ts                 # Legend 类型定义
```

## 🚀 开始实施

现在开始实现第一阶段...

